version: "3"

services:
  migrate-l2:
    image: metis/blockscout
    # depends_on:
    #   - postgres
    build:
      context: ..
      dockerfile: ./docker/Dockerfile
    env_file:
      - envs/l2-local.env
    environment:
      - DATABASE_URL=postgresql://blockscout_app:rEuA9mAU@34.237.142.214:5432/blockscout
    command: mix do ecto.create, ecto.migrate
